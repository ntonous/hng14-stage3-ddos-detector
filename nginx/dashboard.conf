server {
    listen 8080;
    server_name metrics.ntscomputers.dpdns.org;
    
    location / {
        proxy_pass http://hng-detector:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
